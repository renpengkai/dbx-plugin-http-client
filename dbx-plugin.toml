schema_version = 1

[backend]
language = "go"
directory = "backend"
binary = "dbx-plugin-http-client"

[package]
# LICENSE / NOTICE ride along so every distributed .dbxp carries the license copy
# Apache-2.0 section 4(a) requires.
include = ["assets", "ui", "LICENSE", "NOTICE"]
