/dist/
/.dbx-dev/
/.uitest/
# `go build` in ./backend drops the sidecar binary here (name comes from
# [backend] binary in dbx-plugin.toml) — it is a build artifact, never committed.
/backend/dbx-plugin-http-client
.dbx-repository-signing-key.env
.DS_Store
*.log
